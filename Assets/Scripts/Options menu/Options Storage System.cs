using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class OptionsStorageSystem : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    public void saveData()
    {
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
