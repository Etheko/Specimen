using UnityEngine;

public class gameMenuMainScript : MonoBehaviour
{

    // Start is called before the first frame update
    void Start()
    {
        AudioSystemManager.instance.PlayMusic("intro2");
    }

    // Update is called once per frame
    void Update()
    {

    }
}
